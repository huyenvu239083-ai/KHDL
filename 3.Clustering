import pandas as pd
from sklearn.cluster import KMeans

df = pd.read_csv("student-mat.csv")

X = df[['G1', 'G2']]

model = KMeans(n_clusters=3)

df['Cluster'] = model.fit_predict(X)

print(df[['G1', 'G2', 'Cluster']].head(10))
